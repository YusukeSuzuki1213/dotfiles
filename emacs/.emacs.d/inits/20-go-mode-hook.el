;;golang
;(add-hook 'go-mode-hook
;          (lambda ()
;            (setq-default)
;            (setq space-width 4)
;            (setq standard-indent 4)
;            (setq indent-tabs-mode nil)
;            )
;          )