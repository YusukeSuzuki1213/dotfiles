"行番号表示
set number

"検索時に大文字小文字を無視
set ignorecase

"検索結果のハイライト
set hlsearch

" 検索時にファイルの最後まで行ったら最初に戻る
set wrapscan

"タブの幅
set tabstop=4

" タブをスペースに
set expandtab

" 自動的にインデント
set autoindent

" 括弧入力時に対応する括弧を表示
set showmatch

" コマンドライン補完するときに強化されたものを使う
set wildmenu











